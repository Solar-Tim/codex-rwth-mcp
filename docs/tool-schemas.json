{
  "summarize_logs": {
    "description": "Cluster errors, identify likely root causes, and return a compact debugging summary.",
    "inputSchema": {
      "type": "object",
      "required": ["logs"],
      "additionalProperties": false,
      "properties": {
        "logs": {
          "type": "string",
          "description": "Raw logs, traces, terminal output, or CI output to analyze."
        },
        "context": {
          "type": "string",
          "description": "Optional context such as service name, time window, recent change, or failing command.",
          "default": ""
        }
      }
    }
  },
  "summarize_repo": {
    "description": "Summarize repository architecture, code flow, and relevant files/components.",
    "inputSchema": {
      "type": "object",
      "required": ["files"],
      "additionalProperties": false,
      "properties": {
        "files": {
          "type": "string",
          "description": "Concatenated file excerpts with paths. Include only relevant source, config, and docs."
        },
        "question": {
          "type": "string",
          "description": "Optional focus question for architecture, flow, ownership, or relevant components.",
          "default": ""
        }
      }
    }
  },
  "analyze_screenshot": {
    "description": "Extract actionable debugging insights from UI, error, terminal, dashboard, or devtools screenshots.",
    "inputSchema": {
      "type": "object",
      "required": ["image_base64"],
      "additionalProperties": false,
      "properties": {
        "image_base64": {
          "type": "string",
          "description": "Base64-encoded screenshot. PNG is preferred for UI, terminal, and browser screenshots."
        },
        "mime_type": {
          "type": "string",
          "description": "Image MIME type such as image/png or image/jpeg.",
          "default": "image/png"
        },
        "question": {
          "type": "string",
          "description": "Optional diagnostic focus, for example visible error, layout issue, or dashboard anomaly.",
          "default": ""
        }
      }
    }
  },
  "summarize_diff": {
    "description": "Summarize changes, risks, affected areas, and review focus from a large diff.",
    "inputSchema": {
      "type": "object",
      "required": ["diff"],
      "additionalProperties": false,
      "properties": {
        "diff": {
          "type": "string",
          "description": "Unified git diff, PR patch, or copied change set."
        },
        "context": {
          "type": "string",
          "description": "Optional context such as PR title, issue link, release risk, or review focus.",
          "default": ""
        }
      }
    }
  },
  "deep_debug_loop": {
    "description": "Run a bounded KiConnect debugging loop and return a Codex handoff or evidence requests.",
    "inputSchema": {
      "type": "object",
      "required": ["logs"],
      "additionalProperties": false,
      "properties": {
        "logs": {
          "type": "string",
          "description": "Logs, traces, terminal output, or CI output to debug."
        },
        "context": {
          "type": "string",
          "description": "Optional service, repo, recent change, or failure context.",
          "default": ""
        },
        "failing_command": {
          "type": "string",
          "description": "Optional command that failed, if known.",
          "default": ""
        },
        "file_paths": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Optional repo-relative file paths for guarded MCP-side evidence loading.",
          "default": []
        }
      }
    }
  },
  "deep_diff_review": {
    "description": "Run a bounded KiConnect diff-review loop with chunking, merge, and critic phases.",
    "inputSchema": {
      "type": "object",
      "required": [],
      "additionalProperties": false,
      "properties": {
        "diff": {
          "type": "string",
          "description": "Unified diff, PR patch, or copied change set.",
          "default": ""
        },
        "context": {
          "type": "string",
          "description": "Optional PR title, issue link, release risk, or review focus.",
          "default": ""
        },
        "file_paths": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Optional repo-relative file paths for guarded MCP-side evidence loading.",
          "default": []
        }
      }
    }
  },
  "repo_map_loop": {
    "description": "Run a bounded KiConnect repository-mapping loop over provided tree and guarded file evidence.",
    "inputSchema": {
      "type": "object",
      "required": ["question"],
      "additionalProperties": false,
      "properties": {
        "question": {
          "type": "string",
          "description": "Architecture, ownership, flow, or relevant-file question."
        },
        "file_paths": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Optional repo-relative file paths for guarded MCP-side evidence loading.",
          "default": []
        },
        "file_tree": {
          "type": "string",
          "description": "Optional file tree or rg --files output.",
          "default": ""
        },
        "context": {
          "type": "string",
          "description": "Optional repo or task context.",
          "default": ""
        }
      }
    }
  }
}
